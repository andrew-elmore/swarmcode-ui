import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#f44336",
    },
    secondary: {
      main: "#9c27b0",
    },
  },
});

export default theme;
